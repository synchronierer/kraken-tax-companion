# Frontend

The frontend is a strict TypeScript React application built with Vite,
Material UI, and React Router. It provides navigation and presentation only;
domain behavior belongs behind explicit backend APIs.

```bash
npm ci
npm run dev
```
